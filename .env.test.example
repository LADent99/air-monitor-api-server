# Copy to .env.test and adjust if needed.
# These defaults match the docker-compose.yml test DB.
DATABASE_URL="postgresql://airmonitor:airmonitor@localhost:5433/airdb_test"
SHADOW_DATABASE_URL="postgresql://airmonitor:airmonitor@localhost:5433/airdb_shadow"
